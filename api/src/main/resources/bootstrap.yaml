spring:
  cloud:
    propertiesvolume:
      prefixed: false
      enabled: true
      paths: /mnt/secrets/ccpay
      aliases:
        ccpay.api-POSTGRES-PASS: POSTGRES_PASSWORD
        AppInsightsInstrumentationKey: azure.app_insights_key
