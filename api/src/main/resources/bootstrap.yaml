spring:
  cloud:
    propertiesvolume:
      prefixed: false
      enabled: true
      paths: /mnt/secrets/fees-register
      aliases:
        api-POSTGRES-PASS: spring.datasource.password
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
