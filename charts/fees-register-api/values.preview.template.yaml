java:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  aadIdentityName:
  environment:
    POSTGRES_HOST: fees-register-postgres-db-aat.postgres.database.azure.com
    POSTGRES_NAME: fees_register
    POSTGRES_USERNAME: fees_register@fees-register-postgres-db-aat
    POSTGRES_PASSWORD: 5(jfFAHSTOU>0vSs
    POSTGRES_CONNECTION_OPTIONS: "?sslmode=allow"
    SPRING_LIQUIBASE_ENABLED: true
    IDAM_CLIENT_BASE_URL: https://idam-api.aat.platform.hmcts.net
    SPRING_CLOUD_PROPERTIESVOLUME_ENABLED: false
    postgresql:
      postgresqlUsername: fees-register
      postgresqlPassword: fees-register
      postgresqlDatabase: fees-register

  postgresql:
    enabled: true

